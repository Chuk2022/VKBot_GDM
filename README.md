# 🤖 VK Диабет-Ассистент

Бот для ВКонтакте, помогающий отслеживать уровень глюкозы крови и вести дневник самоконтроля при диабете.

## 📋 Содержание
- [Возможности](#-возможности)
- [Структура проекта](#-структура-проекта)
- [Установка и запуск](#-установка-и-запуск)
- [Использование](#-использование)
- [Администрирование](#-администрирование)
- [Технологии](#-технологии)
- [Разработка](#-разработка)
- [FAQ](#-faq)
- [Лицензия](#-лицензия)

## ✨ Возможности

### Для всех пользователей:
- 📝 **Добавление замеров** глюкозы по периодам:
  - 🍽 Перед завтраком
  - 🍽 Перед обедом
  - 🍽 Перед ужином
  - 🌙 Перед сном
  - 🌃 Ночью
  - ⏱ Через час после еды
- 📊 **Просмотр графика** с визуализацией всех замеров
- 📈 **Статистика** (среднее, минимум, максимум, количество записей)
- 🎯 **Целевые уровни** (5.1 и 7.0 ммоль/л)

### Для администратора:
- 👥 **Список всех клиентов** с количеством записей
- 📊 **Просмотр графиков** любого клиента
- 📋 **Общая статистика** по всем пользователям

## 📁 Структура проекта

```
diabetes-bot/
├── bot.py                 # Основной файл бота
├── database.py            # Работа с базой данных
├── config.py              # Конфигурация
├── requirements.txt       # Зависимости
├── Dockerfile            # Для Docker-сборки
├── docker-compose.yml    # Docker Compose
├── .env                  # Переменные окружения
├── .dockerignore         # Исключения для Docker
├── data/                 # База данных
│   └── glucose.db
└── logs/                 # Логи
    └── bot.log
```

## 🚀 Установка и запуск

### Локальный запуск

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/your-username/diabetes-bot.git
cd diabetes-bot
```

2. **Создайте виртуальное окружение:**
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Создайте файл `.env`:**
```env
VK_GROUP_TOKEN=ваш_токен_группы
ADMIN_IDS=12345678,87654321  # ID администраторов через запятую
```

5. **Запустите бота:**
```bash
python bot.py
```

### Запуск в Docker

1. **Соберите образ:**
```bash
docker-compose build
```

2. **Запустите контейнер:**
```bash
docker-compose up -d
```

3. **Просмотр логов:**
```bash
docker-compose logs -f
```

4. **Остановка:**
```bash
docker-compose down
```

## 🎮 Использование

### Первый запуск
1. Напишите боту: `/start`, `старт` или `начало`
2. Бот создаст профиль и покажет главное меню

### Добавление замера
1. Нажмите кнопку с нужным периодом (например, "🍽 Перед завтраком")
2. Введите значение глюкозы (например: `5,6` или `7.2`)
3. Бот подтвердит сохранение и покажет статистику

### Просмотр графика
- Нажмите кнопку "📊 График"
- Бот сгенерирует график с:
  - Точками всех замеров (🔵 обычные, 🟠 через час после еды)
  - Целевыми линиями 5.1 и 7.0 ммоль/л
  - Статистикой справа

## 👑 Администрирование

### Настройка администратора
1. Узнайте свой VK ID (например, через сервис https://regvk.com/id/)
2. Добавьте ID в файл `.env`:
```env
ADMIN_IDS=ваш_id_здесь
```
3. Перезапустите бота

### Возможности администратора
После перезапуска в главном меню появятся дополнительные кнопки:

**👥 Список клиентов**
- Показывает всех зарегистрированных пользователей
- Отображает количество записей у каждого
- При нажатии на клиента показывает его график

**📊 Админ панель**
- Общая статистика по боту
- Количество клиентов и замеров
- Замеры за сегодня

## 🛠 Технологии

- **Python 3.11+**
- **vkbottle** — фреймворк для VK API
- **SQLAlchemy** — ORM для базы данных
- **SQLite** — база данных
- **Matplotlib** — построение графиков
- **NumPy** — математические расчеты
- **Docker** — контейнеризация

## 💻 Разработка

### Требования для разработки
- Python 3.11 или выше
- Git
- (Опционально) Docker

### Установка для разработки
```bash
git clone https://github.com/your-username/diabetes-bot.git
cd diabetes-bot
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
cp .env.example .env  # Создайте .env из примера
# Отредактируйте .env, добавьте токен и свой ID
python bot.py
```

### Тестирование
```bash
# Проверка базы данных
python test_db.py

# Проверка генерации графиков
python test_plot.py
```

## ❓ FAQ

**Q: Как узнать свой VK ID?**  
A: Перейдите на свою страницу ВК — в адресной строке будет `vk.com/id12345678`. Число после `id` — ваш ID.

**Q: Почему не приходят графики?**  
A: Проверьте, что у токена группы есть права на отправку сообщений и фотографий.

**Q: Где хранятся данные?**  
A: В папке `data/glucose.db`. При Docker-запуске данные сохраняются в volume.

**Q: Как сделать другого администратора?**  
A: Добавьте его ID в переменную `ADMIN_IDS` в файле `.env` и перезапустите бота.

**Q: Можно ли использовать на телефоне?**  
A: Да, бот работает через официальное приложение ВК.

## 📄 Лицензия

MIT License. Подробнее в файле [LICENSE](LICENSE).

## 📞 Контакты

По вопросам и предложениям:
- Создайте [issue](https://github.com/your-username/diabetes-bot/issues)
- Напишите в Telegram: [@username](https://t.me/username)

---
⭐ Если проект оказался полезным, поставьте звезду на GitHub!